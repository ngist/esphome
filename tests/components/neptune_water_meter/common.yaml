sensor:
  - platform: neptune_water_meter
    id: water_meter_1
    clock_pin: ${clock_pin}
    enable_pin: ${enable_pin}

uart:
  - id: uart_bus
    rx_pin: ${data_pin}
    baud_rate: 1200
    parity: EVEN
    stop_bits: 2
    data_bits: 7
